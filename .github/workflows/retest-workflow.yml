name: Gird-workflow

on:
  push:
    branches:
      - main

jobs:
  containerise-model:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: docker build -t retest1:latest .

      - name: Push Docker image to Docker Hub
        run: |
          docker tag retest1:latest nelsonsama/retest1:latest
          docker push nelsonsama/retest1:latest

  pull-image-job:
    needs: containerise-model   # dependency on previous job
    runs-on: ubuntu-latest
    steps:
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Pull Docker image
        run: docker pull nelsonsama/retest1:latest

      - name: Run Docker image
        run: docker run -d -p 5000:5000 nelsonsama/retest1:latest
